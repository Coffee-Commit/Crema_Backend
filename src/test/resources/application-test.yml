spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  data:
    redis:
    # host: ${REDIS_HOST}
    # port: ${REDIS_PORT}

jwt:
  secret: "test-jwt-secret-key-that-is-long-enough-for-the-algorithm-to-work"

security:
  oauth2:
    client:
      registration:
        google:
          client-id: "test-id"
          client-secret: "test-secret"
        kakao:
          client-id: "test-id"
          client-secret: "test-secret"