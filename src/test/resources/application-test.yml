spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  data:
    redis:
    # host: ${REDIS_HOST}
    # port: ${REDIS_PORT}

jwt:
  secret: "YS1zdXBlci1sb25nLWFuZC1zZWN1cmUtc2VjcmV0LWtleS1mb3ItdGVzdGluZy1oczI1Ni1hbGdvcml0aG0="

security:
  oauth2:
    client:
      registration:
        google:
          client-id: "test-id"
          client-secret: "test-secret"
        kakao:
          client-id: "test-id"
          client-secret: "test-secret"